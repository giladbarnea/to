[project]
name = "to"
version = "0.1"
description = "Convert or compare between JSON, YAML, TOML and more"
requires-python = ">=3.10"

[project.scripts]
to = "to:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
